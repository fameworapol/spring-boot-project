# 🦷 configuration for upload file
spring:
  #config mail
  mail:
    host: smtp.gmail.com #บอกว่า gmail มี protocol เป็น http
    port: 587
    username: famebazooka #gmail
    password: xcflozvcukzukxul #app password
    properties :
      mail :
        smtp :
          auth : true
          starttls :
            enable : true
  jpa:
    hibernate:
      ddl-auto: update
# 🦷 configuration for connect database
  datasource:
    url: jdbc:postgresql://localhost:5432/backend
    username: postgres
    password: syspass
  servlet:
    multipart:
      enabled: true
      max-file-size: 2MB
      max-request-size: 5MB
---
app :
  token :
    secret : m#MySecretGGWP2021
    issuer : FameServiceBackend
  email:
    from: famebazooka@gmail.com
